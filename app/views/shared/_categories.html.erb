<h1>Categories</h1>
<ul class="categories">
  <% categories.each do |category| %>
    <li>
      <%= link_to category.name, category_path(category) %>
      <strong><%= t("category.questions_html", :count => category.questions_count) %></strong>
    </li>
  <% end %>
</ul>