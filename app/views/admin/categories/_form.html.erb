<%= form_for [:admin, category] do |f| %>
  <%= render "shared/error_messages", :record => category %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit %>
  </p>
<% end %>
